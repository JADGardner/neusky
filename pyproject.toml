[project]
name = "reni_neus"
version = "0.1.0"

dependencies=[
    "icosphere",
    "tqdm",
    "wget",
    "nerfstudio>=0.2.0"
]

[tool.setuptools.packages.find]
include = ["reni_neus*"]

[project.entry-points.'nerfstudio.method_configs']
reni_neus = 'reni_neus.reni_neus_config:RENINeuS'
neus_facto_nerfosr = 'reni_neus.reni_neus_config:NeuSFactoNeRFOSR'
nerfacto_nerfosr = 'reni_neus.reni_neus_config:NeRFFactoNeRFOSR'